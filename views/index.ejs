<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Image Uploader</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&display=swap');
        body {
            font-family: 'Montserrat', sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background: #414040;
            color: #4a4a4a;
            flex-direction: column;
        }

        .container {
            background: #fff;
            padding: 40px 30px;
            border-radius: 20px;
            text-align: center;
            max-width: 450px;
            width: 90%;
            animation: fadeIn 1.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            border: 1px solid rgba(255, 126, 95, 0.1);
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-40px) scale(0.95);
            }

            to {
                opacity: 1;
                transform: translateY(0) scale(1);
            }
        }

        h1 {
            font-size: 32px;
            margin-bottom: 30px;
            font-weight: 700;
            background: linear-gradient(90deg, #ff7e5f, #eb5757);
            -webkit-text-fill-color: transparent;
            margin-top: 0;
            letter-spacing: 1px;
        }

        .form-group {
            width: 100%;
            margin-bottom: 25px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .file-input-hidden {
            width: 0.1px;
            height: 0.1px;
            opacity: 0;
            overflow: hidden;
            position: absolute;
            z-index: -1;
        }

        .custom-file-upload {
            padding: 12px 25px;
            border-radius: 12px;
            cursor: pointer;
            font-size: 16px;
            font-weight: 600;
            display: inline-flex;
            align-items: center;
            gap: 10px;
            width: 90%;
            max-width: 300px;
            justify-content: center;
            background: linear-gradient(135deg, #ff9a85, #ff6c6c);
            color: white;
            border: none;
            box-shadow: 3px 3px 6px rgba(255, 126, 95, 0.4),
                -3px -3px 6px rgba(255, 255, 255, 0.8);
            transition: all 0.3s ease;
        }

        .custom-file-upload:hover {
            transform: translateY(-2px);
            box-shadow: 5px 5px 10px rgba(255, 126, 95, 0.6),
                -5px -5px 10px rgba(255, 255, 255, 0.5);
        }

        .file-name {
            margin-top: 10px;
            font-size: 0.9em;
            color: #888;
            min-height: 1.5em;
        }

        input[type="submit"] {
            padding: 14px 40px;
            background: linear-gradient(135deg, #ff7e5f, #eb5757);
            color: white;
            font-size: 18px;
            font-weight: 700;
            border: none;
            border-radius: 12px;
            cursor: pointer;
            transition: all 0.3s ease;
            width: 90%;
            max-width: 300px;
            margin-top: 25px;
            box-shadow: 5px 5px 10px rgba(255, 126, 95, 0.4);
        }

        input[type="submit"]:hover {
            transform: translateY(-3px) scale(1.02);
            box-shadow: 7px 7px 15px rgba(255, 126, 95, 0.6);
        }

        #uploaded-image-container {
            margin-top: 30px;
            padding-top: 20px;
            /* border-top: 1px solid #f0f0f0; */
            width: 100%;
            display: flex;
            justify-content: center;
            flex-direction: column;
            align-items: center;
            color: #4a4a4a;
        }

        h2 {
            color: #eb5757;
            font-size: 20px;
            margin-bottom: 15px;
        }

        img {
            width: 100%;
            height: auto;
            max-width: 300px;
            max-height: 300px;
            border-radius: 15px;
            border: 5px solid #ff7e5f;
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.2);
            transition: transform 0.3s ease;
        }

        img:hover {
            transform: scale(1.03);
        }

        @media (max-width: 500px) {
            .container {
                padding: 30px 20px;
                border-radius: 15px;
            }

            h1 {
                font-size: 26px;
            }

            .custom-file-upload,
            input[type="submit"] {
                font-size: 15px;
                padding: 12px 20px;
                width: 100%;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <form action="/upload" method="post" enctype="multipart/form-data">
            <div class="form-group">
                <input type="file" name="file" id="file-input">
            </div>
            <div class="form-group">
                <input type="submit" value="upload">
            </div>
        </form>
    </div>
    <div id="uploaded-image-container">
        <% if(url) { %>

            <h2>File Uploaded Successfully</h2>
            <img src="<%= url %>" alt="Uploaded Image">
            <%} %>
    </div>
</body>

</html>